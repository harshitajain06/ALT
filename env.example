# Razorpay (server-side — never commit real values)
# Copy to .env.local and fill in from Razorpay Dashboard → API Keys
RAZORPAY_KEY_ID=
RAZORPAY_SECRET=

# Optional: Razorpay Dashboard → Webhooks → signing secret (for webhook routes only)
RAZORPAY_WEBHOOK_SECRET=
